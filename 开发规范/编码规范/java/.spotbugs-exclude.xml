<?xml version="1.0" encoding="UTF-8"?>
<!--
使用 spotbugs-maven-plugin 插件进行代码静态检测（默认集成与 parent plugs 中），该文件配置检测中忽略 BUG 及相关代码
官方文档：https://spotbugs.readthedocs.io/en/latest/maven.html
xsd 约束：https://github.com/spotbugs/spotbugs/blob/master/spotbugs/etc/findbugsfilter.xsd

## 如何使用
拷贝该文件到需要检测的项目根目录。
执行检测：mvn spotbugs:check
检测结果位于 ${project.build.directory}/spotbugs-site 目录，格式化后阅读，或者使用 mvn spotbugs:gui 采用 UI 方式查看（MAC 高版本无法打开）

-->
<FindBugsFilter>
    <!-- BUG-CODE: https://spotbugs.readthedocs.io/en/stable/bugDescriptions.html -->

    <!-- lombok 默认生成 hashcode 方法最终 return 前进行变量赋值，默认不进行检测 -->
    <Match>
        <Bug pattern="USBR_UNNECESSARY_STORE_BEFORE_RETURN"/>
    </Match>

    <!-- 针对初始化默认不进行同步处理 -->
    <Match>
        <Bug pattern="USFW_UNSYNCHRONIZED_SINGLETON_FIELD_WRITES"/>
    </Match>

    <!-- http://findbugs.sourceforge.net/bugDescriptions.html#EI_EXPOSE_REP
       lombok 默认生成 get、set 方法，对于引用类型数据暂时忽略非安全问题-->
    <Match>
        <Bug pattern="EI_EXPOSE_REP"/>
    </Match>

    <!-- http://findbugs.sourceforge.net/bugDescriptions.html#EI_EXPOSE_REP2
       lombok 默认生成 get、set 方法，对于引用类型数据暂时忽略非安全问题 -->
    <Match>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>

</FindBugsFilter>
